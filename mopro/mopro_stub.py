def generate_proof(job_id, user):
    # Simulate ZK proof generation
    return f"proof_for_{job_id}_{user}"

def verify_proof(proof):
    # Simulate ZK proof verification (mocked)
    return proof.startswith("proof_for_") 